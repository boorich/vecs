# Copy to .env and fill in values
# cp .env.example .env

# Qdrant connection (defaults to localhost if unset)
QDRANT_URL=http://localhost:6333

# Optional: Qdrant API key (only needed if you add auth to your Qdrant instance)
# QDRANT_API_KEY=
